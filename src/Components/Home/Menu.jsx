import SectionStart from "./SectionStart";

const Menu = () => {
    return (
        <div className="my-12">
            <SectionStart heading={"FROM OUR MENU"} subHeading={"---Check it out---"}/>
        </div>
    );
};

export default Menu;